export const addToBasket = (dispatch) => () => {
    return {
        action: 'CLEAR_BASKET',
        payload: {},
    }
}

export const addToBasket = (dispatch) => () => {
    return {
        action: 'CLEAR_BASKET',
        payload: {},
    }
}

export const clearBasket = (dispatch) => () => {
    return {
        action: 'CLEAR_BASKET',
        payload: {},
    }
}
